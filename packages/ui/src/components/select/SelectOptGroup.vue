<script lang="ts">
/**
 * Stub component for <a-select-opt-group>.
 * The new Select uses the `options` prop with `optGroup` entries instead.
 * This exists only for template compatibility.
 */
export default {
  name: 'ASelectOptGroup',
  props: {
    label: { type: String, default: undefined },
    key: { type: [String, Number], default: undefined },
  },
  render() {
    return null
  },
}
</script>
